name: atm_machine
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  lottie: ^3.3.1
  another_flutter_splash_screen: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: any  # <--- Compatible version

flutter_icons:
  android: true
  ios: true
  image_path: "assets/ATM.png"

flutter:
  uses-material-design: true

  assets:
    - assets/ATM.png

